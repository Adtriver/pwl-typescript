function hello(nama: string): string {
    return `Welcome ${nama} di PWL`;
}
const mahasiswa: string = "Hannibal";
const umur: number = 20;

console.log(hello(mahasiswa));
console.log(`Nama: ${mahasiswa}, Umur: ${umur} tahun`);